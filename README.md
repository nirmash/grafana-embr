# Grafana on Embr

Grafana with a Prometheus data source pre-configured. Visualize your OpenTelemetry metrics.

## Deploy to Embr

```bash
embr quickstart deploy <owner>/grafana-embr
```

Set the `PROMETHEUS_URL` environment variable to your Prometheus instance URL.
